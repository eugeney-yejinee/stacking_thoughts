# FvLinker · pH 3 풀림 파이프라인 — 회사에서 돌리는 순서

> 위에서 아래로 그대로 따라가면 된다. **각 단계는 다음 단계를 안전하게 만들려고 있다.**
> 건너뛰면 GPU 시간을 날릴 자리가 정확히 그 단계에 있다.

---

## 0. 설치 (Colab, 2분)

```bash
pip install -q pdbfixer mdtraj openpyxl
pip install -q "openmm[cuda12]"     # ★ GPU 런타임에서만. conda 불필요. CUDA 12 전용
```

- `openmm[cuda12]` 은 2.3 MB짜리 플러그인 휠이다. **conda 도 커널 재시작도 필요 없다.**
- CUDA **13** 빌드는 없다. 런타임이 CUDA 13이면 CPU 로 떨어진다 (아래 [1]에서 알려준다).

```python
from google.colab import drive; drive.mount('/content/drive')
```

경로가 다르면 환경변수로 바꾼다:

```bash
export FVL_DRIVE=/content/drive/MyDrive/FvTwist   # 기본값
export FVL_OUT=$FVL_DRIVE/fvflow                  # ★ 폴더 이름은 fvflow 그대로. 바꾸면 캐시가 고아가 된다
export FVL_IN=$FVL_DRIVE/input
```

---

## 1. 자체 시험 — **GPU 도 Drive 도 필요 없다** (CPU 4~6분)

```bash
python3 tools/fvlinker_ph3md.py --selftest
```

내장 소형계로 전 배선을 밟는다. 확인하는 것:

| 항목 | 통과 기준 |
|---|---|
| 힘장 | `charmm36_2024.xml` + `charmm36_2024/water.xml` 이 잡혀야 한다 (= CHARMM36m) |
| 양성자화 | pH 7.4 → pH 3.0 에서 **순전하가 +5 이상 오른다** |
| 염다리 | pH 7.4 에서 잡히던 것이 pH 3.0 에서 **사라진다** |
| 체크포인트 재개 | 남은 스텝 0 · DCD 프레임 중복 0 |
| 관측 함수 | 51건 전부 통과 |

**여기서 실패하면 그 다음은 볼 것도 없다.** 전체 출력을 그대로 보면 된다 — 잘라내지 않는다.

---

## 2. 사전점검 — 진짜 구조로 배선 확인 + 시간 추정 (GPU 필요)

```bash
python3 tools/fvlinker_ph3md.py --preflight
```

본 실행은 **안 한다.** 찍히는 것:

- 찾은 구성체와 항체 (링커 2종 이상인 항체만 검정 대상이다)
- 계당 **원자 수 · 이황화 개수 · 단백질 순전하**
- **속도 (ns/일)** 와 준비 시간
- 그걸로 계산한 **총 예상 시간**

### 여기서 멈춰야 하는 신호

| 출력 | 뜻 | 할 일 |
|---|---|---|
| `이황화 0개` (잔기 150 넘는데) | 입력 PDB 에 수소가 붙어 있거나 Cys 가 잘렸다 | 입력 확인. 이대로 돌리면 결과가 전부 무의미하다 |
| `순전하 차이 < 5` | Asp/Glu 가 중성화 안 됐다 | **자동으로 멈춘다.** pH 3 이 아니라 pH 7 을 돌리게 되는 상황이다 |
| `등록 플랫폼 ['CPU','Reference']` | GPU 가 안 잡혔다 | 런타임을 GPU 로. `openmm[cuda12]` 재설치 |
| `링커 서열을 못 찾은 구성체` | 엑셀의 링커 서열이 구조 서열에 유일하게 없다 | **주 검정이 못 돈다.** 아래 참고 |

> **링커 서열 문제**: 링커 구간을 서열로 찾는다 (잔기 번호·사슬 나눔에 기대면
> 조용히 엉뚱한 자리를 집는다 — ABangle 이 그렇게 14% 오염됐다). 서열이 구조 안에
> **두 번 이상 나오면 모호해서 안 쓴다.** `(GGGGS)3` 같은 반복 링커가 도메인 안
> 어딘가와 겹치면 여기 걸린다. 그러면 링커 관측값이 전부 NaN 이 되고 출력에 찍힌다.

---

## 3. 본 실행 — 시간을 주면 규모를 알아서 정한다

```bash
python3 tools/fvlinker_ph3md.py --budget-hours 3
```

- 사전점검에서 잰 속도로 **계당 ns 와 씨앗 수를 스스로 정한다.**
- 줄인 것이 있으면 **반드시 찍는다** (조용히 자르지 않는다).
- 양쪽 pH 는 절대 안 줄인다 — 차분이 신호 자체다.
- 계가 하나 끝날 때마다 `results.json` 에 **원자적으로** 저장한다. 중간에 죽어도 거기까지는 남는다.

**끊기면 같은 명령을 다시 치면 된다.** 체크포인트에서 이어간다.

직접 정하고 싶으면:

```bash
python3 tools/fvlinker_ph3md.py --ns 20 --seeds 2
```

> **씨앗을 1로 두지 마라.** MD 는 혼돈계라 궤적 하나는 증거가 아니다.
> 씨앗간 SD 가 차분을 읽는 **분모**다. 시간이 모자라면 ns 를 줄이고 씨앗을 남겨라.

---

## 4. 결과 읽는 법

끝나면 `{FVL_OUT}/ph3md/` 에 넷이 생긴다.

| 파일 | 내용 |
|---|---|
| `results.json` | 계별 원자료 (재실행 시 여기서 이어간다) |
| `within_share.csv` | **먼저 볼 것.** 관측값별 항체내몫 |
| `ph3_delta.csv` | pH 7.4 → 3.0 차분표 |
| `rank_test.csv` | HMW 순위 검정 |

### 읽는 순서 — 이 순서가 중요하다

**① 항체내몫부터.** `y(HMW)를 보기 전에` 찍힌다.

- 0.3 미만 = 그 관측값은 링커가 아니라 **항체 정체**를 재고 있다.
- 지금까지 이 프로젝트의 **모든 축이 여기서 죽었다** (열림분율은 ipTM 과 rho −0.89, 링커신장도는 길이와 rho −0.99).
- 전부 0.3 미만이면 파이프라인이 **거기서 멈춘다.** HMW 와 대조하지 않는다 — 대조하면 항체 효과를 링커 효과로 착각하게 된다.

**② 주 검정 (사전등록된 하나).**

> **주 관측값 = pH 3 에서의 `링커노출`**

기전이 "링커가 끈끈한 면을 덮으면 덜 붙는다" 이고, 설계상 항체 **안에서** 변하는 건
그것뿐이다 (같은 항체면 표면도 패치도 같고 링커만 다르다). 응집이 일어나는 상태가
pH 3 이므로 차분이 아니라 **pH 3 절대값**을 쓴다.

정확 순열 검정이다 (항체 안에서만 섞는다). **다중비교가 없는 단일 검정**이므로 p 를 그대로 읽으면 된다.

**③ 탐색은 탐색이다.** 나머지 관측값의 p 는 가설 생성용이다. 여기서 나온 걸 결과로
보고하면 낚시다 — 다음 라운드에 **사전등록**해서 새 데이터로 확인해야 한다.

---

## 5. 이 파이프라인이 할 수 있는 것과 없는 것

**할 수 있다**
- pH 3 에서 **어떤 염다리가 끊기는지**, 그래서 어느 면이 새로 드러나는지
- 링커가 그 면을 얼마나 덮는지 — 곁사슬 수준에서
- 그걸로 항체 **안에서의** HMW 순위 예측

**못 한다 — 기대하면 안 된다**
- **도메인이 풀리는 것.** 실제 언폴딩은 ms 이상이라 20 ns 로는 안 보인다.
  "풀렸나" 를 보면 아무 일도 안 일어난 것처럼 보이고 잘못된 결론이 난다.
- **두 분자가 실제로 붙는 것.** 전원자 회합 PMF 는 쌍당 약 44 GPU-일이다.
  대신 붙을 확률을 한 분자 문제로 축약했다 (만남 빈도는 링커와 거의 무관하다 — k ∝ R·D, D ∝ 1/R).
- **크기 예측.** 실측이 1회성(100 mL·1차 정제·반복 없음)이라 **순위까지만** 말할 수 있다.
  기울기나 배수를 인용하면 안 된다.

---

## 6. 뭔가 이상할 때

| 증상 | 원인 | 확인 |
|---|---|---|
| 모든 관측값 항체내몫 < 0.3 | 링커가 아니라 항체를 재고 있다 | 정상 동작이다. 멈추는 게 맞다 |
| 링커 관측값이 전부 NaN | 링커 서열을 구조에서 못 찾았다 | 출력의 `링커구간` 을 보라 |
| 이황화 0개 | 입력 PDB 에 수소가 있다 | 수소를 벗겨서 다시 넣어라 |
| `results.json 이 깨졌다` | 쓰는 도중에 죽었다 | `.corrupt` 로 옮기고 처음부터 간다고 알려준다 |
| 예상 시간이 터무니없다 | GPU 가 안 잡혔다 | `등록 플랫폼` 줄을 보라 |

---

## 관련 도구

| 파일 | 용도 |
|---|---|
| `tools/fvlinker_ph3md.py` | 이 파이프라인 |
| `tools/fvobs.py` | 관측값 (SAP · 최대연속패치 · 염다리 · 링커가림) |
| `tools/test_fvobs.py` | 관측값 시험 51건 — 답을 아는 경우로만 |
| `tools/hmw_spread.py` | 노트북 없이 엑셀만 읽어 항체내 HMW 폭을 뽑는다 |
| `tools/fvlinker_model.py` | 별도 통계 모델 (저pH 용출 cis/trans 재짝짓기 분배) |
